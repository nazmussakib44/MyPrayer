import { StyleSheet } from "react-native";

const CurrentDateStyles = StyleSheet.create({
  container: {
    alignItems: 'flex-end',
    marginTop: '15%',
  },
  text: {fontSize: 20, marginLeft: '2%', color: '#666'},
});

export default CurrentDateStyles;
